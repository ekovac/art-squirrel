import { txt } from "../src/filetypes/txt";

describe("Text submission handler", () => {
  it("should extract existing art-squirrel metadata", async () => {
    expect(1).toBe(1);
  });

  it("should export art-squirrel metadata correctly", async () => {
    expect(1).toBe(1);
  });

  it("should preserve existing EXIF tags", async () => {
    expect(1).toBe(1);
  });
});
