import { jpeg } from "../../src/filetypes/jpeg";

describe("JPEG submission handler", () => {
  it("should extract existing art-squirrel metadata", async () => {
    expect(1).toBe(1);
  });

  it("should export art-squirrel metadata correctly", async () => {
    expect(1).toBe(2);
  });
});
